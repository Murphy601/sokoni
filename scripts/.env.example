# Optional — product image automation (scripts/enrich-product-images.mjs)
# Real Google product photos (recommended): free tier at https://serper.dev
# SERPER_API_KEY=

# Pause between image downloads (ms) — avoid rate limits on free AI tier
# IMAGE_FETCH_DELAY_MS=2500

# Public site URL — product images must be reachable here for WhatsApp
# PUBLIC_SITE_URL=https://your-sokoni-site.com
